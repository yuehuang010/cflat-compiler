// The scripts force a zero header-parse budget, so the first C++ header parse must be reported.
// The define keeps this import's cache key private; the refused parse never stores, so the warm
// run parses and reports again.
expect_error("was parsed");
import cpp "cpp_interop_basic.h" define "CFLAT_HEADER_BUDGET_PROBE";
