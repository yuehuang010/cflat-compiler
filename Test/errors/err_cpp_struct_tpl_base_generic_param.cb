import cpp "cpp_interop_tpl.h";

expect_error("multiple bases are not supported yet") {
    struct Bad<T> : cppt.tplbase.Holder<T>, cppt.tplbase.Shape2<T>
    {
    };
}

extern int main() { return 0; }
