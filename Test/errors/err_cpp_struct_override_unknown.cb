import cpp "cpp_interop_poly.h";

expect_error("is marked override but base 'cpppoly.Shape' has no virtual method named missing") {
    struct Bad : cpppoly.Shape
    {
        override int missing() { return 1; }
    };
}

extern int main() { return 0; }
