import cpp "cpp_interop_tpl.h" cache;

expect_error("C++ parameter 'values' of 'cppt.total_init' takes std::initializer_list") {
    extern int main()
    {
        cppi.Tracked value = cppi.Tracked(1);
        return cppt.total_init({value});
    }
}
